# AGENTS.md — Global Execution Directives

## 1. 交互与决策规范 (Interaction & Decisions)
- **【语言要求】**：始终使用**简体中文**进行所有交互、思考、回复与代码注释。
- **【方案先行】**：**修改任何代码前，必须先提供具体方案与改动范围，获得用户明确确认后方可落盘执行。**
- **【暴露假设】**：遇到需求歧义、多种技术路径或隐性权衡时，**必须列出选项供用户决策**，严禁擅自做主。
- **【精炼沟通】**：日常答复 ≤ 3 行；方案陈述直奔核心要点与改动范围，拒绝客套与冗余铺垫。

## 2. 工程与实现准则 (Engineering Principles)
- **【外科手术式修改】**：
  - 只改必须改的代码，**严禁“顺手优化”无关代码或乱动已有格式**。
  - 必须清理自身改动遗留的孤儿 import 与无用变量；原有的死代码仅提示，不擅自删除。
- **【多步骤目标驱动】**：
  - 复杂任务必须在方案中拆解为：`[步骤] → 验证: [检查点 (类型/编译/关键流自审)]`。
  - 增量推进，一步一验，严禁一次性大范围跨步修改。
- **【零测试 & 原生编译必过】**：
  - **严禁生成测试文件、Mock 夹具或搭建测试套件**。
  - 强类型/需编译语言（TS/Java/Go 等）**必须确保类型与编译检查通过，严禁只跑浅层 Lint 糊弄**；无配置项目严格走代码自审。
- **【结构化导航注释】**：
  - 严禁极端：既不写逐行废话注释，也绝不允许长篇代码零注释。
  - 必须具备：模块/组件职能说明、核心代码区块划分（状态/数据转换/事件处理等），以及关键业务分支意图。
- **【YAGNI & 根因修复】**：
  - 能用标准库、CSS、原生 API 解决的，严禁引入新依赖；不写单次使用的抽象与未来灵活性代码。
  - 修 Bug 前必须 grep 所有调用链（callers），在公共分发点做最小防御，杜绝打表面补丁。

## 3. 工具路由与执行红线 (Tools & Circuit Breaker)
- **【本地文件/代码】**：
  - 找路径用 `fffind`；搜符号用 `ffgrep`。查阅 1-2 处匹配后立即切换 `read` 读全貌，严禁连续盲目 grep。
  - 单文件多处改动必须在单次 `edit` 中合并完成；两次 `edit` 匹配失败必须重新 `read`，禁止硬猜。
- **【外部网络/开源检索】**：
  - 快速问答用 `web_search`；深度调研/爬站用 `Tavily`；公开开源实现用 `SearchCode`。
  - 前端 UI 渲染或复杂交互存疑时，用 `chrome-devtools` 截图或求值验证。
- **【死循环熔断器】**：
  - 任何工具调用、文件编辑或编译检查**连续失败 2 次**，必须立即停止盲目重试，向用户说明卡点并请求协助。

## 4. 委派与多 Agent 协作 (Subagents & Council)
- **【主动建议，确认后调度】**：
  - 遇到大型重构、跨模块多文件并行或耗时独立调研时，**Agent 应主动建议启用 `subagent` 并提供分工方案**；
  - **获得用户确认前严禁私自启动**。
- **【写隔离保护】**：多 Agent 协作修改代码时必须启用 `worktree: true`，单个工作区严格保持单写入者。
- **【决策仲裁】**：遇到重大技术选型争议或逻辑死锁时，触发 `council-mode` 顾问团辩论。
